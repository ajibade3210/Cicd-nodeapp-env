packages:
  yum:
    epel-release: []
    chromium: []

commands:
  install_chromium:
    command: |
      if ! which chromium-browser > /dev/null; then
        echo "Chromium not found, installing from source..."
        sudo yum install -y https://path-to-chromium-rpm
      fi
