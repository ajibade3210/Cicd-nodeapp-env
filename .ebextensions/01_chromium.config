commands:
  01_enable_epel:
    command: |
      amazon-linux-extras install epel -y

  02_install_chromium:
    command: |
      wget https://dl.google.com/linux/direct/google-chrome-stable_current_x86_64.rpm -O /tmp/chromium.rpm
      sudo yum localinstall -y /tmp/chromium.rpm
